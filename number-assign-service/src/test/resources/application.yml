serviceUrl: http://localhost:8080
pathToInputFile: C:\Temp\numbers.gz

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
  okhttp:
    enabled: true
  circuitbreaker:
    enabled: true
